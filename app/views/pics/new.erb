<!DOCTYPE html>

<h2> Enter your Pic </h2>

<form method="post" action="/pics/new">
  <div class="input-field">
    <label>Enter image url</label>
    <input type="url" name="path" value="<%= @pic && @pic.path %>">
  </div>
  <div class="input-field">
    <label>Enter image title </label>
    <input type="text" name="pic_title" value="<%= @pic && @pic.pic_title %>">
  </div>
  <div>
    <input type="submit" value="Submit">
  </div>
</form>